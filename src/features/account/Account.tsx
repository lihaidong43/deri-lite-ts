import styled  from 'styled-components';

const AccountWrapper = styled.div`
`
export default function Account(){
  return (
    <AccountWrapper className='account-panel'>
      <h2>Account panel</h2>
      <div></div>
    </AccountWrapper>
  )
}